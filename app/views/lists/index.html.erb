<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/background.png);">
  <div class="container">
    <h1>Welcome to your watchlist</h1>
    <p>Never miss out on your favorite movies again.</p>

    <ul>
      <% @lists.each do |list| %>
        <div class="card-details">
          <h2 class="card-title"><%= list.name %></h2>
          <a class="card-link"><%=link_to 'Details', list_path(list), class:"btn btn-success" %>
        </div>
      <% end %>
    </ul>
    <%=link_to 'Add a new list', new_list_path, class:"btn btn-primary" %>
  </div>
</div>
